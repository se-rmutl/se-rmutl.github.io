// TODO: สร้างและ export ฟังก์ชันอย่างน้อย 3 ฟังก์ชัน
// - setMessage(element, type, text)
// - renderStats(element, stats)
// - renderTasks(element, tasks)
//
// ควรมี empty state เมื่อไม่มีรายการที่ตรงกับ filter
